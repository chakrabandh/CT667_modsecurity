services:
  frontend:
    image: nginx:alpine
    container_name: html-frontend
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    external: true
    name: app-network